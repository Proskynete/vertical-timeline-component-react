import { PropsWithChildren } from 'react';
import { TimelineProps } from '../../interfaces';
declare const Timeline: ({ theme, lang, dateFormat, collapse, withoutDay, descriptionSymbol, customStyles, children, }: PropsWithChildren<TimelineProps>) => JSX.Element;
export { Timeline };
