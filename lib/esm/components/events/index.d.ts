import { PropsWithChildren } from 'react';
import { ContainerProps } from '../../interfaces';
declare const Events: ({ title, startDate, endDate, today, withoutDay, children, }: PropsWithChildren<ContainerProps>) => JSX.Element;
export { Events };
